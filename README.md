```
            ┌──────────────┐
            │      k6      │  ← нагрузочное тестирование
            └──────┬───────┘
                   │
            ┌──────▼───────┐
            │   gateway    │  ← rate limit · timeout · retry · circuit breaker
            └──────┬───────┘    + экспортёр метрик Prometheus
                   │
            ┌──────▼───────┐
            │    nginx     │  ← балансировщик (least_conn)
            └──────┬───────┘
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
   ┌─────────┐┌─────────┐┌─────────┐
   │ post-   ││ post-   ││ post-   │  ← N реплик stateless-сервиса
   │ service ││ service ││ service │    (CRUD, cursor/offset пагинация,
   └────┬────┘└────┬────┘└────┬────┘     кеш Redis)
        │         │         │
        └─────────┼─────────┘
                  │
        ┌─────────┴─────────┐
        ▼                   ▼
  ┌──────────┐        ┌──────────┐
  │ Postgres │        │  Redis   │
  └──────────┘        └──────────┘

           ┌────────────────┐
           │   autoscaler   │  ← опрашивает Prometheus,
           └────────────────┘    

           ┌────────────────┐
           │   Prometheus   │  ← сбор метрик с gateway
           └────────────────┘
           ┌────────────────┐
           │    Grafana     │  ← визуализация
           └────────────────┘
```
